---
description: What this stack actually invokes, and what it never does
---

Run `ccatlas usage --unused`.

If the output says usage is unavailable, recommend nothing — an unread
transcript is not an unused stack. Counts are exact; token figures are
estimates.
