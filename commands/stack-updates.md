---
description: Stale pins, available upgrades and marketplace staleness
---

Run `ccatlas updates`. Lead with stale pins if any — the version string has not
moved but the source has, so `/plugin update` reports nothing while old code
runs. An entry listed as "behind the install" is not an upgrade.
