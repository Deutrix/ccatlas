---
description: Write a self-contained HTML report of this stack
---

Run `ccatlas report --redact` unless the user says the file stays local. The
unredacted form contains absolute project paths, which carry the username and
client or product names.
