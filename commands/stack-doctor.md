---
description: Findings for this stack, each with a severity, a cause and the exact fix command
---

Run `ccatlas doctor`. Report findings most-severe first and quote each
`fixCommand` verbatim — never invent one. Also report the "Not checked" list:
checks that did not run are not a clean bill of health.
