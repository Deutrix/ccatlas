# IDE
.idea/
.vscode/

# Node (for when the scaffold lands — T0.8)
node_modules/
dist/
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Local
.env
.env.*
*.log

# build artifact — rebuilt by prepack/pretest
bin/
dist/

# Generated reports. These embed the local username, hostname and project
# paths — never a source artefact, and never something to commit.
ccatlas-report.html
*-report.html
reports/

# Internal design documents. Kept on disk, not published — the repo ships the
# product, not the planning. See the README for what they were.
docs/

# Release runbook. Internal process, not part of the shipped product.
PUBLISHING.md
