---
description: SYNTHETIC FIXTURE — a command contributed by the synthetic @skills-dir plugin. Never invoke it.
---

Synthetic fixture command.

Relevant to `docs/FORMATS.md` §0 trap #16: a command is recorded in transcripts
**bare** (`/synthetic-report`), with no plugin namespace. Owner resolution must
go through the inventory. A command with this same bare name is also defined as
a personal command in `fixtures/synthetic/scale/tree/home/.claude/commands/` —
a partial fixture for the §5 gap "a command name colliding across two plugins"
(this collision is personal-vs-plugin; plugin-vs-plugin remains open).
